# 前置依赖清单

来源：`docs/PRD-v1.2.md` §12。当前状态统一初始化为“待提供”；只有取得可验证的文件、地址或运行结果后才能更新状态。

| §12 项目 | 前置依赖原文 | 必须到位节点 | 当前状态 |
|---|---|---|---|
| 1 | Z-Image、MiniMax H3 ref2v 两个 API 格式工作流 JSON + 节点绑定信息(§8),MiniMax 工作流须含可注入的 duration 输入(秒),并明确参考图输入数量与留空行为(不支持留空则提供可变参考数版本)。 | Z-Image 部分：C007 开工前；MiniMax H3 部分：C009 开工前；两者均为 C012 验收前置 | 待提供 |
| 2 | 4 个提示词模板初始内容(可后补,先用占位示例)。 | C002/C005/C006/C007/C009 可用占位示例开工；4 个正式模板须在 C012 验收前到位 | 待提供 |
| 3 | vLLM 以 `--enable-sleep-mode` 启动并验证 sleep/wake 可用。 | C007 开工前 | 待提供 |
| 4 | Postgres DSN、Comfy 与 vLLM 地址端口。 | Postgres DSN：C001 验收前；vLLM 地址端口：C005 开工前；Comfy 地址端口：C007 开工前 | 待提供 |

