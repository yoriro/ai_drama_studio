# 前置依赖清单

来源：`docs/PRD-v1.2.md` §12。当前状态统一初始化为“待提供”；只有取得可验证的文件、地址或运行结果后才能更新状态。

| §12 项目 | 前置依赖原文 | 必须到位节点 | 当前状态 |
|---|---|---|---|
| 1 | Z-Image、MiniMax H3 ref2v 两个 API 格式工作流 JSON + 节点绑定信息(§8),MiniMax 工作流须含可注入的 duration 输入(秒),并明确参考图输入数量与留空行为(不支持留空则提供可变参考数版本)。 | Z-Image 部分：C007 开工前；MiniMax H3 部分：C009 开工前；两者均为 C012 验收前置 | 待提供 |
| 2 | 4 个提示词模板初始内容(可后补,先用占位示例)。 | C002/C005/C006/C007/C009 可用占位示例开工；4 个正式模板须在 C012 验收前到位 | `script2assets` 已提供：2026-08-27 在 C005 正式模板验收库经设置 API 读回并与 C005 spec §5.1 逐字一致；`script2shots` 已提供：2026-08-28 正式验收 API 读回内容与 C006 spec §5.1 逐字一致（1181 字符），真实 `gen_shots` 已走通；`zimage`、`minimaxh3` 仍待提供，C012 前置尚未全部满足 |
| 3 | vLLM 以 `--enable-sleep-mode` 启动并验证 sleep/wake 可用。 | C007 开工前 | 部分满足：2026-08-27 C005 已验证真实 `/wake_up` 返回 200 并完成封闭 schema 调用；尚无 `/sleep` 与完整 sleep/wake 分时证据，C007 开工门槛仍为待验证 |
| 4 | Postgres DSN、Comfy 与 vLLM 地址端口。 | Postgres DSN：C001 验收前，并在此后每个使用 PostgreSQL 的 change 验收前提供可验证环境；vLLM 地址端口：C005 开工前；Comfy 地址端口：C007 开工前 | Postgres DSN 已提供：本机 PostgreSQL 5432；C001 专用空库 `ai_drama_studio_c001_acceptance_20260824` 已完成 C001 迁移验收；2026-08-25 C002 专用验收库 `ai_drama_studio_c002_acceptance_20260824` 已验证 `6b8e3f0a1d24 (head)`、`alembic check` 无待生成操作、完整 `pytest` 为 `3 passed`，且 T15/T16 浏览器复审通过；2026-08-25 C003 专用验收库 `ai_drama_studio_c003_audit_20260825` 已验证 `6b8e3f0a1d24 (head)`、`alembic check` 无待生成操作、两条授权用例 `2 passed`、完整 `pytest` 为 `5 passed`；2026-08-25 C004 专用验收库 `ai_drama_studio_c004_audit_20260825` 已通过队列与完整回归复审；2026-08-27 C005 再次复审使用相互独立的 `ai_drama_studio_c005_reaudit_20260827` 与 `ai_drama_studio_c005_acceptance_20260826`：隔离库从空库迁移至 `6b8e3f0a1d24 (head)`，`alembic check` 无新增操作，C005 定向用例 `9 passed`、完整后端 `18 passed`，测试后业务/任务行清理为 0；正式库同为 head，已有 15 条真实 done 任务与 5 条 generated 资产。2026-08-28 C006 Sol 复审在隔离库 `ai_drama_c006_reaudit_acd92530` 验证 `6b8e3f0a1d24 (head)`、`alembic check` 无新增操作、完整后端 `32 passed in 18.12s`，并发探针确认 impact/replacement 锁定有效，验收后隔离库已删除；正式 API/vLLM 均返回 200，模型为 `Qwen3-30B-A3B-Instruct-2507-AWQ-4bit`，已有两条真实 `gen_shots` done。默认库 `ai_drama_studio` 仍停留在 C001 revision；Comfy 地址端口仍待 C007 |
